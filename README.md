# README
@lineage: readme
@about: The Absolute Microkernel & Zero-Trust State Engine (Layer-0)
@generator: gemini-3.1-pro
@topos: Core Holographic Manifold & Deterministic Axioms

`xphi` (formerly Theoria) is the fundamental Layer-0 manifold. It establishes the absolute physical laws, cryptographic determinism, and spatial topology that govern the entire ecosystem. While higher-dimensional boundaries (`brane`) and correction agents (`surgent`) project and mutate these structures for specific operational scopes, `xphi` remains the immutable core—managing true zero-trust WebAssembly execution, topological state inversions, and XOR causal parity.

---

## Topos Dimensions

The architecture is partitioned into three absolute foundational planes. These are the root axioms from which all higher-level encapsulation and structural orchestration emanate.

### `arch` (Structural Axioms & Cryptographic Topology)

The absolute fundamental laws of the manifold. It defines the deterministic data structures, cryptographic guarantees, and the mathematical geometry of the network space.

* **`xor` (Deterministic Parity & Lineage):** The cryptographic heart. Enforces RFC 8785 (JCS) determinism (`sign.signature`). Manages the mathematical recovery of spatiotemporal data via tripartite XOR parity and orchestrates the absolute state lineage (DAG) through immutable blocks (`block.parser`, `store.residue`).
* **`contract` & `gov` (Atomic Truths):** Defines the irreducible smart contract interfaces and atomic event buses (`event.psi`, `event.tunnelbus`). The `gov` module establishes the core alignment and gating logic (`gov.gate`, `gov.aligner`) that `surgent` later metabolizes for meta-governance.
* **`topos` (Spatial Geometry):** Maps the physical network topology. Manages root anchor protocols (`anchor.protocol`), spatial edge transitions (`edge.trajectory`), and establishes the foundational boundary models (`bound.tunnel`, `bound.sensor`) that `brane` extends for external integration.

### `phase` (State Transition & WASM Membrane)

The active thermodynamic execution environment. It governs how potential energy (intents) transitions into kinetic reality (state evolution) through isolated, deterministic constraints.

* **`wasm` (Zero-Trust Sandbox & Fuel):** The deterministic `dphi.wasm` execution core. Isolates untrusted payloads completely from the host OS. Enforces strict instruction-level resource management via dynamic Fuel injection (`wasm.wasmcg`) and asynchronous brokering (`wasm.broker`).
* **`runtime` & `executor` (Daemon Dynamics):** The core lifecycle engine. Orchestrates the genesis bootstrap (`runtime.daemon.bootstrap`), manages asynchronous task supervisors (`task.supervisor`), and metabolizes raw execution dynamics within isolated swarms (`executor.swarm`).
* **`ator` & `bind` (Resonance & Rhythm):** The evaluative alignment plane. Judges execution transcripts (`ator.transcript`, `resonance.judgment`) to ensure state transitions match topological laws. Manages the cyclical pulse and network folding mechanisms (`bind.rhythm.psi`, `bind.folding`) to maintain sync across the manifold.

### `watcher` (Kernel State & Dimensional Observability)

The omniscient internal observation loop and topological kernel. It does not merely log data; it evaluates spatial tension, aggregates state nodes, and controls topological inversions.

* **`kernel` (State Collapse & Inversion):** The absolute memory of the manifold. Aggregates disparate state mutations (`state.aggregator`), maintains the genesis configurations (`state.node0`), and triggers profound topological phase shifts (`kernel.inversion`) when systemic resonance thresholds are breached.
* **`xe` (Tension & Field Dynamics):** The thermodynamic observer. Continuously evaluates spatial tension (`xe.topos.tension`), particle interactions (`xe.topos.particle`), and continuous field dynamics (`xe.cont`) without requiring external active polling.
* **`plane` & `tracer` (Surface Flow & Auditing):** Routes and regulates internal data flows across the system’s surfaces (`plane.surface.tunnel`). Traces structural trajectories (`tracer.trajectory`) and strictly audits core WASM execution trajectories (`auditor.wasm`) to secure the Layer-0 boundary against dimensional collapse.